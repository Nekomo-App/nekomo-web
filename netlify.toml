# Netlify deployment config for Nekomo.
# The official Next.js runtime handles App Router, SSR, API routes, and images.
# Set ADMIN_KEY + AUTH_SECRET (and any other vars) in Site → Environment variables.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
